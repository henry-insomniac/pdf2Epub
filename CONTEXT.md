# PDF 转 EPUB

本上下文描述将 PDF 源内容转化为 EPUB 出版物时使用的领域语言。V1 聚焦单一转换方向，不承担通用电子书格式互转。

## Language

**PDF 转 EPUB 转换**:
从 PDF 源文档生成 EPUB 出版物的领域行为。V1 中不指代其他输入格式或输出格式之间的转换。
_Avoid_: 电子书格式互转、通用格式转换

**可转换 PDF**:
正文具有可提取文本，因而能在不识别页面图像中文字的前提下转化为 EPUB 的 PDF。文件可以包含封面、插图、图表等非正文图像。
_Avoid_: 任意 PDF、扫描 PDF

**图书型 PDF**:
以单栏连续正文为主，并由章节标题、段落、列表、普通插图和少量简单表格组成的可转换 PDF。V1 的正文为横排简体中文、繁体中文、英文或中英混排；双栏论文、公式密集教材、复杂排版杂志、表单、漫画、竖排文字及从右向左排版不属于此范围。
_Avoid_: 任意文档、复杂版式 PDF

**扫描型 PDF**:
全部或部分正文只存在于页面图像中，必须先识别图像中的文字才能获得可重排正文的 PDF。扫描型 PDF 可以生成固定版式 EPUB，但在没有 OCR 时不能生成可重排 EPUB。
_Avoid_: 带可靠文本层的 PDF、OCR PDF

**受保护 PDF**:
需要密码才能打开、采用加密保护或明确禁止提取内容的 PDF。受保护 PDF 不属于 V1 的可转换 PDF。
_Avoid_: 损坏 PDF、扫描型 PDF

**可重排 EPUB**:
内容流能够随阅读设备、窗口和字号变化而重新排布的 EPUB 3 出版物。它保留内容结构和阅读顺序，不复刻 PDF 的页坐标与固定分页。
_Avoid_: 固定版式 EPUB、PDF 页面副本

**固定版式 EPUB**:
每个 PDF 源页面作为一个固定视口页面保留，并声明左右页和双页展开语义的 EPUB 3 出版物。用于正文主要存在于页面图片中或无法可靠重建阅读顺序的复杂版式 PDF；它保留页面外观，但不承诺文字调字号、搜索或复制能力。
_Avoid_: 可重排 EPUB、OCR EPUB

**自动版式选择**:
转换前抽样检查文本层、跨页重复内容和主页面图片覆盖率；正文文本可靠时选择可重排 EPUB，正文主要位于页面图片中时选择固定版式 EPUB。用户仍可明确指定输出版式。
_Avoid_: 阅读器单双页显示设置、盲目降级

**导航目录**:
EPUB 中供阅读器展示和跳转的章节层级。它优先来自 PDF 大纲，也可以来自高置信度的章节标题推断，但不等同于正文中的印刷目录页。
_Avoid_: PDF 大纲、印刷目录页

**印刷目录页**:
作为 PDF 正文页面排版出来的目录内容。它在转换后仍属于正文，但自身不直接充当 EPUB 导航目录。
_Avoid_: 导航目录、PDF 大纲

**转换任务**:
用户针对一个可转换 PDF 发起的一次 PDF 转 EPUB 转换。转换任务经历等待、转换中，并最终成功、失败或由用户取消；成功后产生一个可下载的 EPUB。
_Avoid_: 同步请求、批量任务

**访客主体**:
公开额度 Beta 中由服务端签名 Cookie 标识的钱包身份。额度、订单、转换任务与下载都绑定其 `subject ID`；它不是共享管理员账号或完整注册用户，持有首次兑换的额度码可以在新浏览器恢复同一 subject。
_Avoid_: 匿名请求、管理员、邮箱账号

**转换额度**:
公开模式中用于授权创建转换任务的整数单位。任务被服务端接受时扣除 1 次，失败或取消时退回；浏览器展示余额不构成账本事实。
_Avoid_: 现金余额、前端计数器、支付金额

**上传票据**:
ALTCHA 或 Turnstile 通过服务端验证后签发的短时、一次性、绑定访客主体的随机凭证。上传接口在读取 PDF body 前消费票据。
_Avoid_: 人机验证 token、长期 API key、CSRF token

**额度码钱包**:
由 HMAC 签名额度码首次兑换时创建或绑定的访客钱包。额度码只增加一次额度，之后再次输入只恢复该钱包；完整额度码是 bearer recovery credential，持有者拥有钱包访问权。
_Avoid_: 可重复充值码、公开订单号、普通优惠券

**支付订单**:
服务端创建并绑定访客主体、固定 Stripe Price 和额度数量的待支付记录。只有验签且绑定一致的支付回调能将订单转为已支付并写入额度流水。
_Avoid_: 支付成功页面、Checkout URL、前端价格

**成功任务**:
已处理全部源页面、确定主要阅读顺序、生成并通过规范校验的 EPUB 的转换任务。成功任务可以包含转换警告，但不能缺失正文。
_Avoid_: 部分成功、不完整输出

**转换警告**:
成功任务中未造成正文缺失或 EPUB 失效的可见降级说明，例如局部内容转为图片或脚注未建立跳转。
_Avoid_: 转换失败、静默降级
